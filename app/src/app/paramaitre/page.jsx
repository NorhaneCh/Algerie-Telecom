import React from 'react'
import { styles } from '../../styles'

const page = () => {
    return (
        <div className={`${styles.paddingX} ${styles.paddingY}`}>
            paramaitre
        </div>
    )
}
 export default page